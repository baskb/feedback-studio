# Demo session screenshots — mobile (2026-06-13)

Real phone captures from a live `--demo --tunnel` session ("Roastly" sample page over a
Cloudflare quick tunnel, processed remotely via Claude Code remote control). Raw and
uncropped — crop the Android status bar / keyboard before publishing.

| File | Shows | Best use |
|---|---|---|
| `voice-dictation-listening.jpg` | Composer mid-dictation: mic live, "Listening… (English (US))", words appearing | THE voice-on-phone money shot — README hero, launch posts |
| `hero-pins-and-composer.jpg` | Numbered pins on the hero + open composer over the tunnel URL | "Pins on a real page, from a phone" overview |
| `composer-improve-type.jpg` | Type selector with **Improve** active, finished comment text | Explaining the fix/change/improve latitude model |
| `touch-picker-dom-walk.jpg` | The ▲/▼ DOM-walk touch picker on a card (`div.card` chip + Comment button) | The mobile precision-picking feature — unique, demo it |
| `composer-change-weekends-only.jpg` | A **Change** comment being typed on the lorem-ipsum card | The "user asks, agent applies near-verbatim" story |
| `remote-control-tunnel-start.jpg` | Claude Code remote control on the phone: tunnel URL handed over, "Process feedback" queued | The full phone-only loop, part 1 |
| `remote-control-process-feedback.jpg` | Remote control processing the round (reading comments.json, working) | The full phone-only loop, part 2 — pairs with part 1 |

Story arc for a thread/demo video storyboard: tunnel start → pins on page → pick element
by touch → speak a comment → type selector → "process feedback" from the phone → pins
flip green. The still-missing asset for the README is the screen-recorded GIF of that arc
(PROGRESS.md next-up #1); these stills are the storyboard for it.
